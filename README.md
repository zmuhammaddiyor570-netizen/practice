#PRAKTIKA PROJECT

Learing GIT and Python Foundation  

## GIT COMMSNDS:
```
git config --global user.name
git config --global user.email
git init
git satatus
git add . 
git commit -m "feat: Initial Setup"
```